<?xml version="1.0" encoding="utf-8" ?> 
<sqlMap namespace="PackageTypeCustomer" xmlns="http://ibatis.apache.org/mapping" 
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <alias>
        <typeAlias alias="PackageTypeCustomer" type="Book.Model.PackageTypeCustomer, Book.Model" />
    </alias>
    <resultMaps>                                    
        <resultMap id="PackageTypeCustomerResult" class="PackageTypeCustomer">
			<result property="PrimaryKey" column="PrimaryKey" />
			<result property="PackageTypeId" column="PackageTypeId" />
			<result property="CustomerId" column="CustomerId" />
			<result property="Customer" column="CustomerId" lazyLoad="false" select="Customer.select_by_primary_key" />
			<result property="PackageType" column="PackageTypeId" lazyLoad="false" select="PackageType.select_by_primary_key" />
        </resultMap>
    </resultMaps>
		
    <statements>
		<select id="has_rows_of" resultClass="bool">
			select case when exists(
				SELECT
					*
				FROM
					[dbo].[PackageTypeCustomer]
				WHERE
				[PrimaryKey] = #PrimaryKey#
				) then 1 else 0 end
		</select>
		<select id="has_rows" resultClass="bool">
			select case when exists(
				SELECT
					*
				FROM
					[dbo].[PackageTypeCustomer]
 								
				) then 1 else 0 end
		</select>
		<!-- select by primary key -->
        <select id="select_by_primary_key" resultMap="PackageTypeCustomerResult" parameterClass="PackageTypeCustomer">
			SELECT
				[PrimaryKey],
				[PackageTypeId],
				[CustomerId]
			FROM
				[dbo].[PackageTypeCustomer]
			WHERE
				[PrimaryKey] = #PrimaryKey#
        </select>
		
		<!-- delete -->
        <delete id="delete" parameterClass="string">
			DELETE FROM [dbo].[PackageTypeCustomer]
			WHERE
				[PrimaryKey] = #PrimaryKey#
        </delete>

		<!-- select all -->
        <select id="select_all" resultMap="PackageTypeCustomerResult" >
			SELECT *
			FROM 
				[dbo].[PackageTypeCustomer]
			ORDER BY PrimaryKey ASC
        </select>
        
		<select id="count_all" resultClass="int">
			select count(*)
			from 
				[dbo].[PackageTypeCustomer]
		</select>
		
		<select id="select_all_with_paging" resultMap="PackageTypeCustomerResult">
            SELECT *
            FROM
            (
                SELECT *, row_number() over(order by $OrderStatement$) rownumber
                FROM    
                    [dbo].[PackageTypeCustomer]
            ) t
            WHERE t.rownumber between #Row1# and #Row2#
		</select>
		
		<!-- insert -->
        <insert id="insert" parameterClass="PackageTypeCustomer">
			INSERT INTO [dbo].[PackageTypeCustomer] 
			(
				[PrimaryKey],
				[PackageTypeId],
				[CustomerId]
			) 
			VALUES 
			(
				#PrimaryKey#,
				#PackageTypeId#,
				#CustomerId#
			)
        </insert>
		
		<!-- update -->
        <update id="update" parameterClass="PackageTypeCustomer">
			UPDATE [PackageTypeCustomer] SET
				[PackageTypeId] = #PackageTypeId#,
				[CustomerId] = #CustomerId#
			WHERE
				[PrimaryKey] = #PrimaryKey#
        </update>
		
		
    </statements>
</sqlMap>

