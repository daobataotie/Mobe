<?xml version="1.0" encoding="utf-8" ?> 
<sqlMap namespace="wfrecord" xmlns="http://ibatis.apache.org/mapping" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <statements>
   


    <!--待我审批-->
    <select id="select_myexaming" resultMap="wfrecordResult" parameterClass="string">
      SELECT *
      FROM
      [dbo].[wfrecord] where
      nowprocessid in
      (select processId from accepterattrib where accepterid=#value# and acceptertype='个人')
      and allstate='进行中'
    </select>
    
    
  </statements>
</sqlMap>