<?xml version="1.0" encoding="utf-8" ?> 
<sqlMap namespace="PackageType" xmlns="http://ibatis.apache.org/mapping" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <statements>
  <select id="all_byCustomer" resultMap="PackageTypeResult" parameterClass="string">
    <![CDATA[
			SELECT 
				*
			FROM
				[dbo].[PackageType]
        WHERE
				PackageTypeId 
        in (select PackageTypeId from PackageTypeCustomer where CustomerId=#value# )			
			]]>
  </select>
  </statements>

</sqlMap>